package no.uib.inf101.sem2.model;

import no.uib.inf101.sem2.grid.Grid;

public class DanmakuField extends Grid {
  
  /**
   * DanmakuFIeld constructor is used to create a field boundary
   * where all sprites can move on. 
   * Dimensions is set by a Grid class.
   */
  public DanmakuField(int x, int y, int width, int height) {
    super(x, y, width, height);
    
  }
   
}
